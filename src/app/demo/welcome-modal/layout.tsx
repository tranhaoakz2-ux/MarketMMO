export const metadata = {
  title: "Demo: Popup chào mừng — MaketMMO",
  robots: { index: false, follow: false },
};

export default function WelcomeModalDemoLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return children;
}
