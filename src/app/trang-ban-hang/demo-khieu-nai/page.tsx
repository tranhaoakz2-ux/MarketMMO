import DemoDisputes from "@/components/seller-demo/DemoDisputes";

// DEMO redesign — Khiếu nại. Dữ liệu giả, không đụng trang thật.
export default function DemoDisputesPage() {
  return <DemoDisputes />;
}

export const metadata = { title: "Demo · Khiếu nại — MarketMMO" };
